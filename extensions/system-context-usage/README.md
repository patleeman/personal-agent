# Context Usage system extension

Shows the current conversation's context window usage in the composer status bar.

The host supplies precomputed token usage through `statusBarContext.contextUsage`; this extension renders the compact circular indicator and hover label.
