# Composer Attachments

System extension that owns the visible composer attachment control. Core still owns the hidden file input and file ingestion plumbing; this extension calls `openFilePicker()` through the composer control context.
