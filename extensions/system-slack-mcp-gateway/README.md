# Slack MCP Gateway

Slack MCP Gateway is an experimental system extension that connects Slack channels to Personal Agent conversations through the Slack MCP server.

It is disabled by default. Enable `system-slack-mcp-gateway` from the Extensions page before connecting Slack, then use the Slack MCP Gateway page to authenticate, save a channel ID, and attach that channel to a conversation.
