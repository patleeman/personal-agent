# Git Status system extension

Shows the active workspace branch and git diff summary in the composer status bar.

This extension owns the composer git indicator. The host supplies the current conversation/workspace status through `statusBarContext`; the extension only renders the status bar item.
