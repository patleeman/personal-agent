# Excalidraw input extension

Provides the composer drawing tool as a system extension.

The extension contributes a composer input tool that opens an Excalidraw modal and returns a serialized scene plus PNG preview to the host composer.
