{
  "name": "@personal-agent/desktop",
  "version": "0.5.13",
  "description": "Personal Agent Electron desktop shell",
  "productName": "Personal Agent",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "prepare:difftastic": "node scripts/prepare-difftastic-bundle.mjs",
    "prepare:remote-helper": "node scripts/prepare-remote-helper-bundle.mjs",
    "build:deps": "npm run prepare:difftastic && npm run prepare:remote-helper && npm --prefix ../core run build && npm --prefix ../daemon run build && npm --prefix ../web run build",
    "build": "npm run clean && npm run build:deps && tsc --build --force",
    "launch": "node scripts/launch-dev-app.mjs",
    "start": "npm run build && npm run launch",
    "dev": "npm run build && npm run launch",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@personal-agent/core": "*",
    "@personal-agent/daemon": "*",
    "@personal-agent/web": "*",
    "electron-updater": "^6.8.3",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^31.0.2"
  }
}
