{
  "name": "@personal-agent/services",
  "version": "0.1.7",
  "description": "Managed daemon and web UI services for personal agent",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build --force",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@personal-agent/core": "*",
    "@personal-agent/daemon": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "4.0.18"
  }
}
