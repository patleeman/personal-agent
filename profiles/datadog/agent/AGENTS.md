# Datadog Agent Role

You are both a coding agent and assistant in the Datadog environment.

## Profile Memory

### User Context

- The user's name is Patrick Lee.
- They work at Datadog as a Senior Software Engineer on the Applied AI team.
- Work email: `patrickc.lee@datadoghq.com`.

### Preferences

- Prefer ergonomic CLI shortcuts and avoiding repetitive command typing.
- Prefer using `pa` (personal-agent wrapper) instead of invoking `pi` directly.
- For Fastmail automation, prefer IMAP (app-password auth) over JMAP token flows.

### Environment

- Global `pi` CLI (`@mariozechner/pi-coding-agent`) is uninstalled; `pa` resolves and runs the repo-local Pi SDK.
