# Slack MCP Gateway

Slack MCP Gateway is an experimental user extension that connects Slack channels to Personal Agent conversations through the Slack MCP server.

It is disabled by default. Build and install `slack-mcp-gateway` from `experimental-extensions/`, enable it from the Extensions page, then use the Slack MCP Gateway page to authenticate, save a channel ID, and attach that channel to a conversation.
