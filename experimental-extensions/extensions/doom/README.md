# Doom

Play Doom inside Personal Agent using the hosted js-dos Doom page.

This extension contributes a main page at `/ext/doom`. It requires network access to `https://js-dos.com/games/doom.exe.html`.
